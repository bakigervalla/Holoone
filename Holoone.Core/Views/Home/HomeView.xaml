<UserControl x:Class="Holoone.Core.Views.Home.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Holoone.Core.Views.Home"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Style="{StaticResource HomeLevelButtons}">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--<Image Opacity="0.1" Margin="0,40" Stretch="Uniform"
            Source="pack://application:,,,/Holoone.Core;component/Resources/Images/holo_one_logo.png" />-->

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center">
            <!--<Button Content="Login"
                    x:Name="btnMinus" 
                    cal:Message.Attach="[Event Click]=[Action Minus(left.Text, right.Text):result.Text]" />-->

            <Button x:Name="ShowLoginPage">Login</Button>
            <Button Margin="10 0">Anchors</Button>
            <Button x:Name="ShowExportPage">Export</Button>
        </StackPanel>

    </Grid>
</UserControl>
