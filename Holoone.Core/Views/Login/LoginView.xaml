<UserControl x:Class="Holoone.Core.Views.Login.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Holoone.Core.Views.Login"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Image Height="80" VerticalAlignment="Center" 
               Source="pack://application:,,,/Holoone.Core;component/Resources/Images/holo_one_logo_login.png" />

        <StackPanel Grid.Row="1" VerticalAlignment="Top" Orientation="Vertical" Style="{StaticResource LoginLevelButtons}">
            <Button x:Name="ShowLoginSpherePage" VerticalAlignment="Top">Login with Sphere</Button>
            <Button x:Name="ShowLoginThinkRealityPage" Margin="0 10" >Login with ThinkReality</Button>
        </StackPanel>

    </Grid>

</UserControl>
