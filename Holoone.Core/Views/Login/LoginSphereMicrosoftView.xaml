<UserControl x:Class="Holoone.Core.Views.Login.LoginSphereMicrosoftView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Holoone.Core.Views.Login"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBox x:Name="UserName" />
            <PasswordBox Grid.Row="1" x:Name="Password" />
        </StackPanel>

        <StackPanel Grid.Row="1">
            <RadioButton GroupName="Region" Content="USA" />
            <RadioButton GroupName="Region" Content="Europe" />
            <RadioButton GroupName="Region" Content="China" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Button >Login</Button>
            <Button >Sign in with Microsoft</Button>
        </StackPanel>
    </Grid>

</UserControl>
