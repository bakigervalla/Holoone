<UserControl x:Class="HolooneNavis.Views.Export.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HolooneNavis.Views.Export"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Image Height="80" VerticalAlignment="Center" 
               Source="pack://application:,,,/HolooneNavis;component/Resources/Images/export.png" />
                        
        <StackPanel Grid.Row="1" VerticalAlignment="Top" Orientation="Vertical" Style="{StaticResource LoginLevelButtons}">
            <Button x:Name="ShowExportDefaultPage" VerticalAlignment="Top">Default</Button>
            <Button x:Name="ShowExportBIMPage" Margin="0 10" >BIM</Button>
        </StackPanel>

    </Grid>
    
</UserControl>
